import React from 'react';
import { Footer } from './components/Footer';

const App: React.FC = () => {
    return (
        <main className="w-screen min-h-screen">
            <Footer />
        </main>
    );
};

export default App;
